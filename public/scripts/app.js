$(() => {

  $('.tabs').tabs(); //Tabs on reviews page

  $('.datepicker').datepicker({
    firstDay: 2
  }); //date picker on New and edit forms

  // const options = {
  //   autoClose: true,
  //   firstDay: 1
  // };
  //
  // var elem = document.querySelector('.datepicker');
  // M.Datepicker.init(elem, options);

});
